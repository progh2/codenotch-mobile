name: codenotch_mobile
description: "Codenotch mobile companion: home-screen AI usage widgets."
publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  home_widget: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
